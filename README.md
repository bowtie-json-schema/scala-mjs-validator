# scala-mjs-validator

A [Bowtie](https://github.com/bowtie-json-schema/bowtie) test harness.

Its image is published to `ghcr.io/bowtie-json-schema/scala-mjs-validator` and run via `bowtie run -i scala-mjs-validator`.
